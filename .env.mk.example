# Example local Makefile configuration
# Copy to .env.mk and adjust for your environment
# See LOCAL_DEVELOPMENT.md for full documentation

# Path to mooR source checkout (default: ../.. assumes cowbell is in moor/cores/cowbell/)
# MOOR_DIR = /path/to/moor

# Build profile: debug or release (default: debug)
# BUILD_PROFILE = release

# Build mode: cargo, direct, or docker (default: cargo)
# MOORC_TYPE = direct

# Daemon data directory (default: local-daemon-data)
# DATA_DIR = my-data

# Database filename (default: development.db)
# DB_NAME = mydb.db
